import { PendingEvidence } from "./PendingEvidence";

export function Timeline() {
  return (
    <PendingEvidence
      sectionId="timeline"
      num="03"
      label="Logbook"
      headline="Experience timeline — verification in progress."
      note="Every role, date, and outcome on this timeline will be confirmed against real records before publication. Rather than publish an approximate history, this section is intentionally sealed. Reach out for the current CV or a direct conversation about background and projects."
    />
  );
}
